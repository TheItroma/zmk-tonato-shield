if SHIELD_TONATO_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Tonato"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_TONATO_LEFT || SHIELD_TONATO_RIGHT

config ZMK_SPLIT
    default y

endif
